


<section class='clip-detail-header'> 

	<a {{action 'play'}} class='item-icon play clickable'> </a> 

	<div class='item-info'>
		<h1>{{title}}</h1>
        <span class='duration'>{{pretty-duration duration}}</span><br />
        Published {{ago pub_date}}
	</div>

</section>

<section>
	<p>{{description}}</p>

</section>

<section>
	{{#if session.isAuthenticated}}
     <a class="" data-toggle="modal"  {{action 'openModal' 'modals/add-clip' model lists}}>+ Add to playlist</a>
{{else}}
	<strong>Please {{#link-to 'login'}}log in{{/link-to}} to add this to a playlist.</strong>
{{/if}}</section>